# NOTE: Releases are currently done locally via `make release-dry` / goreleaser.
# Re-enable when ready to move back to GitHub Actions.
#
# name: Release
#
# on:
#   push:
#     tags:
#       - "v*"
#
# permissions:
#   contents: write
#
# jobs:
#   release:
#     name: Release
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0
#
#       - uses: actions/setup-go@v5
#         with:
#           go-version: "1.25"
#
#       - name: Run tests
#         run: go test ./... -race -timeout 60s
#
#       - name: Run GoReleaser
#         uses: goreleaser/goreleaser-action@v6
#         with:
#           version: "~> v2"
#           args: release --clean
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
